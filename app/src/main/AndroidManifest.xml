<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 添加queries声明，解决Android 11+包可见性问题 -->
    <queries>
        <!-- 相机应用 -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <!-- 图库应用 -->
        <intent>
            <action android:name="android.intent.action.PICK" />
            <data android:mimeType="image/*" />
        </intent>
        <!-- 文件管理器 -->
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
            <data android:mimeType="image/*" />
        </intent>
    </queries>

    <!-- 权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 日历权限（设置用药提醒用） -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- 相机硬件特性 -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIHealth_ALL"
        android:name=".AiHealthApplication"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:theme">

        <!-- 登录界面 - 作为启动页 -->
        <activity
            android:name=".ui.activity.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AIHealth_ALL"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.AIHealth_ALL"
            android:screenOrientation="portrait" />
        <!-- 启动页（可选，不再作为启动器） -->
        <activity
            android:name=".ui.activity.SplashActivity"
            android:exported="false"
            android:theme="@style/Theme.AIHealth.Splash"
            android:screenOrientation="portrait" />

        <!-- 主页面 - 登录后进入 -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.AIHealth_ALL"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait" />

        <!-- 诊断相关Activity -->
        <activity
            android:name=".ui.activity.CameraActivity"
            android:exported="false"
            android:label="拍照"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AIHealth_ALL" />

        <!-- 药品管理相关Activity -->
        <activity
            android:name=".ui.activity.DrugCycleManagerActivity"
            android:exported="false"
            android:label="药品周期管理"
            android:theme="@style/Theme.AIHealth_ALL"
            android:screenOrientation="portrait" />

        <!-- 饮食分析相关Activity -->
        <activity
            android:name=".ui.activity.DietAnalysisActivity"
            android:exported="false"
            android:label="饮食分析"
            android:theme="@style/Theme.AIHealth_ALL"
            android:screenOrientation="portrait" />

        <!-- 可视化Activity -->
        <activity
            android:name=".ui.activity.VisualizationActivity"
            android:exported="false"
            android:label="数据可视化"
            android:theme="@style/Theme.AIHealth_ALL"
            android:screenOrientation="portrait" />

        <!-- 广播接收器 - 药品提醒 -->
        <receiver
            android:name=".receiver.DrugCycleReceiver"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.REBOOT" />
                <action android:name="com.aihealth.DRUG_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- 添加FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>